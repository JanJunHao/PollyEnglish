// 复制本文件为 Secrets.xcconfig（同目录），填入真实 token + base URL 后再 build。
// Secrets.xcconfig 已被 .gitignore 排除，不会进版本库。

ANTHROPIC_AUTH_TOKEN = PUT_YOUR_TOKEN_HERE
ANTHROPIC_BASE_URL = https:/$()/airouter.appaam.com/api
